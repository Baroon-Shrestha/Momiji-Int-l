[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[identity]
  invite_redirect = "/admin/#"
  recovery_redirect = "/admin/#"
  email_change_redirect = "/admin/#"
